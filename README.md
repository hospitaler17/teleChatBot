# teleGemmaBot

Telegram-бот — мост к Google Gemma API. Отвечает на запросы пользователей из заданного списка чатов и личных диалогов. В групповых чатах реагирует только на прямые обращения (`@bot_username` или ответ на сообщение бота).

## Возможности

- 🤖 Интеграция с Google Gemma через Generative AI SDK
- 🔒 Контроль доступа: списки разрешённых пользователей и чатов
- 👤 Администрирование через Telegram-команды
- ⚙️ Конфигурация через YAML-файлы и переменные окружения
- 🐳 Развёртывание в Docker

## Быстрый старт

### Требования

- Python 3.11+
- Telegram Bot Token ([BotFather](https://t.me/BotFather))
- Google API Key ([AI Studio](https://aistudio.google.com/))

### Установка

```bash
# Клонировать репозиторий
git clone https://github.com/hospitaler17/teleGemmaBot.git
cd teleGemmaBot

# Установить зависимости
pip install -r requirements.txt

# Скопировать примеры конфигурации
cp .env.example .env
cp config/config.example.yaml config/config.yaml
cp config/allowed_users.example.yaml config/allowed_users.yaml
```

Отредактируйте `.env`, `config/config.yaml` и `config/allowed_users.yaml` под свои нужды.

### Запуск

```bash
python -m src.main
```

### Docker

```bash
# Сборка и запуск
docker compose up -d
```

## Конфигурация

| Переменная окружения | Описание |
|---|---|
| `TELEGRAM_BOT_TOKEN` | Токен бота Telegram |
| `GOOGLE_API_KEY` | Ключ Google AI API |

Остальные настройки — в `config/config.yaml` (модель, параметры генерации, админы) и `config/allowed_users.yaml` (списки пользователей и чатов).

## Команды бота

| Команда | Описание |
|---|---|
| `/start` | Приветствие |
| `/help` | Справка |
| `/admin_add_user <id>` | Добавить пользователя |
| `/admin_remove_user <id>` | Удалить пользователя |
| `/admin_add_chat <id>` | Добавить чат |
| `/admin_remove_chat <id>` | Удалить чат |
| `/admin_list` | Показать списки доступа |

Команды `/admin_*` доступны только администраторам, указанным в `config.yaml`.

## Тесты

```bash
pip install -r requirements-dev.txt
pytest --cov=src
```

## Структура проекта

```
src/
├── main.py                  # Точка входа
├── api/
│   └── gemma_client.py      # Клиент Google Gemma API
├── bot/
│   ├── bot.py               # Сборка приложения бота
│   ├── filters/
│   │   └── access_filter.py # Контроль доступа
│   └── handlers/
│       ├── admin_handler.py  # Команды администратора
│       ├── command_handler.py# /start, /help
│       └── message_handler.py# Обработка текстовых сообщений
└── config/
    └── settings.py           # Загрузка конфигурации
```

## Лицензия

MIT