# teleChatBot

Telegram-бот — мост к Mistral API. Отвечает на запросы пользователей из заданного списка чатов и личных диалогов. В групповых чатах реагирует только на прямые обращения (`@bot_username` или ответ на сообщение бота).

[![CI](https://github.com/hospitaler17/teleChatBot/actions/workflows/ci.yml/badge.svg)](https://github.com/hospitaler17/teleChatBot/actions/workflows/ci.yml)

## Возможности

- 🤖 Интеграция с Mistral AI через официальный SDK
- 🧠 **Умный выбор модели** — автоматический анализ запроса и выбор оптимальной модели Mistral
- 💬 **Потоковая отправка сообщений** — ответы генерируются постепенно для лучшего пользовательского опыта
- 📝 История разговора — бот помнит контекст и предыдущие сообщения (до 10 пар по умолчанию)
- 🌐 Умный веб-поиск с автоматическим выбором провайдера (Google → SearXNG → DuckDuckGo)
- 🔒 Контроль доступа: списки разрешённых пользователей и чатов
- 👤 Администрирование через Telegram-команды
- 🖥️ **CLI режим** — интерактивная командная строка для отладки и тестирования
- ⚙️ Конфигурация через YAML-файлы и переменные окружения
- 🐳 Развёртывание в Docker

## Быстрый старт

### Требования

- Python 3.11+
- Telegram Bot Token ([BotFather](https://t.me/BotFather))
- Mistral API Key ([console.mistral.ai](https://console.mistral.ai/))
- (Опционально) Google Custom Search API для лучшего качества поиска

### Установка

```bash
# Клонировать репозиторий
git clone https://github.com/hospitaler17/teleChatBot.git
cd teleChatBot

# Установить зависимости
pip install -r requirements.txt

# Скопировать примеры конфигурации
cp .env.example .env
cp config/config.example.yaml config/config.yaml
cp config/allowed_users.example.yaml config/allowed_users.yaml
```

Отредактируйте `.env`, `config/config.yaml` и `config/allowed_users.yaml` под свои нужды.

### Запуск

```bash
python -m src.main
```

### Docker

```bash
# Сборка и запуск
docker compose up -d
```

### CLI режим (для отладки)

Для тестирования системного промпта и отладки без Telegram можно запустить бота в режиме командной строки:

1. Включите CLI режим в `config/config.yaml`:
   ```yaml
   bot:
     cli_mode: true
   ```

2. Запустите бота:
   ```bash
   python -m src.main
   ```

**Доступные команды в CLI:**
- `/help` — показать справку
- `/stats` — показать статистику разговора
- `/clear` — очистить историю разговора
- `/exit` или `/quit` — выйти из CLI

**Особенности CLI режима:**
- ✅ Сохраняет историю разговора (как в Telegram)
- ✅ Поддерживает все функции: умный выбор модели, веб-поиск, контекст даты
- ✅ Не требует Telegram токен
- ✅ Удобно для тестирования и отладки системных промптов

## Конфигурация

| Переменная окружения | Описание |
|---|---|
| `TELEGRAM_BOT_TOKEN` | Токен бота Telegram |
| `MISTRAL_API_KEY` | Ключ Mistral AI API |
| `GOOGLE_API_KEY` | (Опционально) Ключ Google Custom Search для лучшего качества поиска |
| `GOOGLE_SEARCH_ENGINE_ID` | (Опционально) ID поискового движка Google |

Остальные настройки — в `config/config.yaml` (модель, параметры генерации, веб-поиск, админы) и `config/allowed_users.yaml` (списки пользователей и чатов).

### Потоковая отправка сообщений

Бот поддерживает **потоковую отправку сообщений** — ответы генерируются и отправляются постепенно, обеспечивая лучший пользовательский опыт при работе с длинными ответами. Это особенно полезно для сложных запросов, требующих развернутых ответов.

**Настройки в `config/config.yaml`:**

```yaml
bot:
  enable_streaming: true  # Включить потоковую отправку
  streaming_threshold: 100  # Минимальная длина для активации стриминга (символы)
  streaming_update_interval: 1.0  # Интервал обновлений (секунды)
```

**Параметры:**
- **`enable_streaming`**: Включает/выключает потоковую отправку (по умолчанию: `true`)
- **`streaming_threshold`**: Минимальная длина ответа для активации стриминга в символах (по умолчанию: `100`). Ответы короче этого порога отправляются без потокового обновления, после полной генерации
- **`streaming_update_interval`**: Интервал между обновлениями сообщения в секундах (по умолчанию: `1.0`). Меньшие значения = более частые обновления, но больше вызовов Telegram API. Рекомендуется: 0.5-2.0 секунды. **Примечание**: Telegram имеет ограничение на частоту редактирования сообщений (~30 редактирований/сек для всех чатов). При большом количестве одновременных пользователей рекомендуется использовать интервал 1.0 секунды или больше

**Как это работает:**
1. Бот начинает получать ответ от AI модели по мере его генерации
2. Первое сообщение отправляется, как только накоплено достаточно текста (>100 символов)
3. Сообщение обновляется с заданным интервалом, показывая прогресс генерации
4. Для очень длинных ответов автоматически разбивается на несколько сообщений

### Веб-поиск

Бот использует **умную систему веб-поиска с автоматическим fallback**:

1. **Google Custom Search** (если настроен) - лучшее качество результатов
2. **SearXNG** (публичный инстанс) - надёжная альтернатива без регистрации
3. **DuckDuckGo** - запасной вариант

#### Быстрый старт (без регистрации)

Веб-поиск работает сразу через SearXNG и DuckDuckGo! Просто установите `enable_web_search: true` в `config/config.yaml`.

#### Настройка Google (опционально, для лучшего качества)

Для использования Google Custom Search (100 запросов/день бесплатно):

1. Получите API ключ: [developers.google.com/custom-search](https://developers.google.com/custom-search/v1/overview)
2. Создайте Search Engine: [programmablesearchengine.google.com](https://programmablesearchengine.google.com/)
3. Добавьте ключи в `.env`:
   ```
   GOOGLE_API_KEY=ваш-api-ключ
   GOOGLE_SEARCH_ENGINE_ID=ваш-id-движка
   ```

Бот автоматически использует веб-поиск для запросов о:
- 📰 Новостях и текущих событиях
- 🌤️ Погоде
- 💱 Ценах и курсах валют
- 📊 Актуальной информации

Поиск активируется автоматически при обнаружении ключевых слов в запросе.

### Умный выбор модели

Бот использует **алгоритм динамического выбора модели** на основе анализа запроса. Вместо статического использования одной модели, система автоматически выбирает наиболее подходящую модель из бесплатного тарифа Mistral AI:

#### Доступные модели:

1. **mistral-small-latest** — быстрая модель для простых запросов
   - ⚡ Самая быстрая
   - 💡 Подходит для: простые вопросы, перевод, общение
   
2. **codestral-latest** — специализированная модель для кода
   - 💻 Оптимизирована для программирования
   - 💡 Подходит для: написание кода, отладка, объяснение алгоритмов
   
3. **pixtral-12b-latest** — мультимодальная модель
   - 🖼️ Поддержка изображений
   - 💡 Подходит для: анализ изображений, OCR, визуальные вопросы
   
4. **mistral-large-latest** — мощная модель для сложных задач
   - 🧠 Максимальные возможности
   - 💡 Подходит для: сложное рассуждение, длинный контекст, детальный анализ

#### Автоматический выбор:

Система анализирует каждый запрос по следующим критериям:
- **Содержание кода** → `codestral-latest`
- **Наличие изображений** → `pixtral-12b-latest`
- **Сложное рассуждение** → `mistral-large-latest`
- **Длинный контекст (>20k токенов)** → `mistral-large-latest`
- **Простые запросы** → `mistral-small-latest` (по умолчанию)

Примеры:
```
"Привет, как дела?" → mistral-small-latest
"Напиши функцию на Python" → codestral-latest
"Проанализируй пошагово почему это работает" → mistral-large-latest
```

Модель по умолчанию задаётся в `config.yaml` и используется как fallback.

### История разговора

Бот автоматически сохраняет историю разговора для каждого пользователя. Это позволяет ему:
- ✅ Ссылаться на предыдущие сообщения в диалоге
- ✅ Поддерживать контекст многотурных диалогов
- ✅ Помнить информацию о пользователе в пределах сессии

**Конфигурация:**

```yaml
# config/config.yaml
mistral:
  conversation_history_size: 10  # Сохранять последние 10 пар сообщений
```

Или через переменную окружения:
```
MISTRAL_CONVERSATION_HISTORY_SIZE=15
```

## Команды бота

| Команда | Описание |
|---|---|
| `/start` | Приветствие |
| `/help` | Справка |
| `/admin_add_user <id>` | Добавить пользователя |
| `/admin_remove_user <id>` | Удалить пользователя |
| `/admin_add_chat <id>` | Добавить чат |
| `/admin_remove_chat <id>` | Удалить чат |
| `/admin_list` | Показать списки доступа |

Команды `/admin_*` доступны только администраторам, указанным в `config.yaml`.

## Тесты

```bash
pip install -r requirements-dev.txt
pytest --cov=src
```

## Структура проекта

```
src/
├── main.py                  # Точка входа
├── api/
│   ├── mistral_client.py    # Клиент Mistral API с динамическим выбором модели
│   ├── model_selector.py    # Алгоритм выбора модели на основе анализа запроса
│   ├── conversation_memory.py # Управление историей разговоров
│   └── web_search.py        # Веб-поиск с мульти-провайдерным fallback
├── bot/
│   ├── bot.py               # Сборка приложения бота
│   ├── filters/
│   │   └── access_filter.py # Контроль доступа
│   └── handlers/
│       ├── admin_handler.py  # Команды администратора
│       ├── command_handler.py# /start, /help
│       └── message_handler.py# Обработка текстовых сообщений
├── cli/
│   └── cli_chat.py          # Интерактивный CLI режим для отладки
└── config/
    └── settings.py           # Загрузка конфигурации
```

## Лицензия

[GPL-3.0](LICENSE)
